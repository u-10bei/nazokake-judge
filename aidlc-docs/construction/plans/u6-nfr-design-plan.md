# U6 NFR Design Plan — 層拡張 + 事前生成割当

**ユニット**: U6。U6-NFR-01〜22 を設計パターン **DP-U6-NN** と論理コンポーネント **LC-U6-NN** に落とす。
**前提（既決）**: FD 全 7 問（BR-U6-01〜20）/ NFR Req 全 5 問 A + 追加 7 点（U6-NFR-01〜22 / TSD-U6-01〜10）。
**性格**: 設計の中心は **「割当を実行時から設計時へ移す」構造**と、**migration の安全性**。新奇な部品は導入せず、既存の層構造（`scripts` → `schema` / `backend` → `domain`）に収める。

このドキュメントは **Part 1（Plan + 質問）**。回答・承認後に `nfr-design-patterns.md`（DP-U6-NN）/ `logical-components.md`（LC-U6-NN + 依存方向）を生成します。

## 生成予定の成果物（Part 2）→ 生成済み（2026-07-20, 全 5 問 A + 追加 5 点）
- [x] `construction/u6/nfr-design/nfr-design-patterns.md`（DP-U6-NN + 非採用部品表）
- [x] `construction/u6/nfr-design/logical-components.md`（LC-U6-NN + 依存方向）

---

## 調査で判明した設計の前提（実装確認済み・2026-07-20）

| # | 事実 | 設計への含意 |
|---|---|---|
| 1 | **`scripts` は `backend` を一切 import していない**（`schema` と `scripts.*` のみ・全 CLI で確認） | `plan_generate`（scripts）が使う定数は **`schema` に置くしかない** → **`POOL_LAYERS` の配置が決定づけられる**（Q1） |
| 2 | **`pool_sufficiency` は「充足判定の唯一の実装」**（DP-U4a-05 / U4a-NFR-10。ingest と issue の両方が呼ぶことで述語の乖離を構造的に防ぐ） | `POOL_LAYERS` への置換は**この 1 関数の中で完結**させる（述語の単一性を壊さない） |
| 3 | **巡回グラフ `C_n(1..d)` は距離 1 の輪を含むため必ず連結** | **プラン全体の連結性（BR-U6-10 ②）は構成的に保証できる**（検証は保険） |

### ★ 層間比率は「円周上の item 配置」で決まる（実測）

巡回グラフ `C38(1..6)`（228 辺）で、層の並べ方だけを変えた実測:

| 円周配置 | 層間ペア | 比率 | ゲート（≥0.65） |
|---|---:|---:|---|
| **層ごとに固める**（grouped） | 89 | **0.390** | ❌ **不足** |
| **層をインターリーブ** | 166 | **0.728** | ✅ 充足 |
| **シャッフル**（seed 固定） | 176 | **0.772** | ✅ 充足 |

→ **同じ辺集合・同じ正則次数でも、item の並べ方だけで層間比率がゲートを割る**。**配置は生成アルゴリズムの必須要件**であり、「たまたま通る」に委ねてはいけない（Q2）。

---

## 設計パターン適用性評価（U6）
| 論点 | 適用 | 方針 |
|---|---|---|
| **設計時への移動**（実行時ロジックの除去） | **適用（最重要・U6 固有）** | 事前生成 + 引き当て。実行時に抽選を残さない。→ Q3 |
| **構成による保証 vs 生成後検証の切り分け** | **適用（核心）** | 構成で保証できるもの（gap=0・全体連結・同一ペア0）と、検証が要るもの（ブロック連結・k・層間）を分ける。→ Q2 |
| **明示リストによる暗黙走査の置換** | **適用** | `for layer in Layer` → `POOL_LAYERS`。層値追加で要求が自動増殖する構造を断つ。→ Q1 |
| **危険操作のガード**（activate 切替） | **適用** | 収集開始後の切替を拒否。→ Q5 |
| キャッシュ / キュー / CB / ロック / スケール | **N/A** | プラン引き当ては PK 参照・運用者の逐次操作。 |

---

## 質問（回答は各 `[Answer]:` に記入。特記なき場合 **★A=推奨デフォルト**）

### Q1【`POOL_LAYERS` の配置と「非空要求」の分離】
**事実 #1** より、`plan_generate`（scripts）が層フィルタを使う以上、定数は **`schema` に置くしかない**（`backend` は scripts から見えない）。問うのは**責務の分割**。

- **★A（推奨）**: **`schema` に 2 つの定数を分けて置く**:
  - **`POOL_LAYERS`** = `(pro, ai, edit, rule, anchor)` — **充足判定の母数**（`practice` を除外）
  - **`REQUIRED_LAYERS`** = `(pro, ai, edit, rule)` — **非空を要求する層**
  - **分ける理由**: 両者は**別の概念**。`anchor` は**母数には入る**（層間ペアの相手になる・出題される）が、**非空を強制すると `anchor` 不在のプール（ドライラン・将来の構成変更）でゲートが落ちる**。下帯アンカーは**研究上の要請**であって**割当アルゴリズムの成立条件ではない**（4 層あれば層間ペアは組める）。
  - `pool_sufficiency` は **`POOL_LAYERS` で母数を絞り、`REQUIRED_LAYERS` で非空を検査**する。
- **B**: `POOL_LAYERS` 1 つで両方を兼ねる（`anchor` の非空も要求）。→ 単純だが、**ドライランで `anchor` を用意しないとゲートが落ちる**。`anchor` の欠落を「アルゴリズムの不成立」と扱うのは意味論的に不正確。
- **C**: `backend/domain` に置く。→ **`scripts` から import できない**（事実 #1）。不採用。

[Answer]: **A ＋ 研究側組成チェックの置き場を明確化**。`POOL_LAYERS`（母数）/ `REQUIRED_LAYERS`（非空要求）の分離、および「**`anchor` 非空は研究上の要請でありアルゴリズム成立条件ではない**」の意味論に同意。
  **裏返しの手当て（BR-U6-22 として FD に追補済み）**: `anchor` を非空要求から外すと、**`anchor` 投入忘れのまま本番プランが生成される事故をランタイムのゲートが拾わなくなる**。この検査の正しい置き場は**ランタイムではなく `plan_generate` CLI** — **期待組成（n=38・層別件数 10/2/14/9/3）を引数で受け取り、実プールとの不一致で明示失敗**する。BR-U6-21 の制約ファイルと同じ「**研究側入力**」の一部として自然に載る。

### Q2【★プラン生成】構成による保証と生成後検証の切り分け
BR-U6-10 の①〜⑥を、**構成で保証する**ものと**生成後に検証する**ものに分ける。

- **★A（推奨）**: **構成で保証できるものは構成で保証し、残りを検証する**:

  | 制約 | 保証方法 |
  |---|---|
  | ① 露出 gap=0 | **構成**（m-正則グラフ＝全頂点が次数 m） |
  | ② 全体連結 | **構成**（巡回グラフ `C_n(1..d)` は距離 1 の輪を含むため必ず連結・事実 #3） |
  | ④ 同一ペア 0 | **構成**（全辺が相異なる＝集合として構成） |
  | ⑤ 層間比率 ≥0.65 | **構成（配置）+ 検証**。**層をインターリーブして円周に配置**する（実測: grouped 0.390 ❌ / interleave 0.728 ✅）。配置は必須要件とし、**結果は必ず検証**する |
  | ③ k 制約 ≤3 | **検証**（分割の貪欲配分に依存）。満たすまで**seed を進めて再試行**（決定論は「seed → プラン」で保つ） |
  | ⑥ ブロック連結 | **検証**（分割に依存・BR-U6-20）。同上 |

  - **生成 CLI は「構成 → 検証 → 失敗なら seed を進めて再試行 → 上限回数で明示失敗」**とする。**決定論は保たれる**（同一 seed から同じ試行列を辿る）。
  - **①②④を構成で押さえるのが要**: 検証だけに頼ると「反例が出たが直し方が分からない」状態になる。
- **B**: すべて探索（ランダム生成 + 検証 + リトライ）。→ **層間比率が配置依存であることを見落とすと永久に収束しない**（grouped 配置では 0.390 で構造的に不可能）。
- **C**: すべて構成で保証（分割まで解析的に）。→ k 制約とブロック連結を同時に満たす分割の解析的構成は過剰。

[Answer]: **A ＋ 内容制約チャネルの追加（★最重要・BR-U6-21 として FD に追補済み）**。
  **①〜⑥は形式制約（グラフ理論側）だけで閉じており、タスク5 §3 で確定済みの内容制約が生成器仕様から欠落していた**。純粋な巡回グラフ + 自由配置では表現できないものが 4 種:
  | 種別 | 例 | 巡回グラフとの関係 |
  |---|---|---|
  | **辺の禁止（ハード）** | pivot 音衝突ペアの直接対戦禁止（全46音照合） | **円周距離 >6 に配置すれば構成的に排除**（隣接は距離 ≤6）。**PU6-8**（禁止辺の不在）を検証に追加 |
  | **辺の忌避（ソフト）** | N7×C-6 準同音、かんこう/かんそう | 同上・**ソフト扱い**（排除できなくても失敗させずレポート出力） |
  | **辺の濃縮** | N8 vs 結節 9 件を厚めに（診断標本） | 12-正則の枠内で N8 の 12 辺のうち何本を結節側に充てるかは**配置で決まる** → **配置探索の目的関数** |
  | **提示順の回避** | ①④少年隣接・N6×新作③ の同一評価者内連続提示回避 | **辺集合でなくペア列の順序**の制約 → **分割後に順序付け（sequencing）ステップが 1 段必要** |
  **設計への含意**: ★A の「構成 → 検証 → リトライ」の骨格は正しいが、**配置は単なるインターリーブではなく制約付き探索**になり、**分割後に順序付けステップが 1 段増える**。**制約の中身は研究側（タスク5）の成果物、器は U6** という切り分けを保つため、**CLI が制約ファイル（禁止ペア・忌避ペア・濃縮目標・隣接回避グループ）を入力に取る**形とする。seed リトライの決定論はそのまま成立。
  **実測（成立性確認済み）**: 禁止辺 8 本・濃縮対象 9 件で近傍探索（seed 固定）→ **禁止辺違反 0 / 層間 0.706（≥0.65 充足）/ 濃縮 9-9 本を同時達成**。
  **追加**: 監査再現用に **初期 seed + 成功試行番号（または最終有効 seed）の両方**をプランメタに記録（Part 2 で確定）。

### Q3【引き当ての LC 構造】プランをどこで読み、フォールバックをどこで分岐するか
`session.py:53`（`generate_pairs` の唯一の呼び出し）を置換する。

- **★A（推奨）**: **Repository に読み取りを、サービス層に分岐を置く**:
  - `Repository.get_active_plan_set()` → 有効な `plan_set`（`is_active=1`）
  - `Repository.get_plan_pairs(plan_set, plan_index)` → そのスロットのペア列（練習含む）
  - `Repository.get_token_plan_index(token)` → `plan_index`（NULL 可）
  - **`session.start_or_resume` の新規分岐で**: `plan_index` が **NULL なら従来の `generate_pairs`**（フォールバック, U6-NFR-14）/ **非 NULL ならプランを引く**。
  - **`save_pair_sequence` 以降は一切変更しない**（Session + PairSequence + `likert_targets` の同一 batch 原子保存＝U5 DP-U5-02 を維持）。
  - **`generate_pairs` は無改修で残る**（BR-U6-17）。呼ばれるのは**フォールバック経路のみ**。
- **B**: プラン引き当てを `domain` に置く。→ **domain が repo に依存**＝層の逆流。不採用。
- **C**: `session.py` が D1 を直接触る。→ Repository を I/O 境界に保つ既存方針に反する。

[Answer]: **A**（追加なし）。Repository=読み取り / サービス層=分岐 / `save_pair_sequence` 以降不変 / フォールバックは NULL 分岐のみ——**U5 の原子保存（DP-U5-02）を触らない切り方**が正しい。「**ペア列（練習含む）**」の明記により、**Q2 の提示順制約がプラン内の `pair_index` 順として運ばれる構造**も整合する。

### Q4【脱落時の引き継ぎ粒度】（FD からの持ち越し・BR-U6-15）
補充トークンが**何を引き継ぐか**を確定する。

- **★A（推奨）**: **未消化ペアのみを引き継ぐ**。補充トークンには**元トークンが未回答のペアだけ**を `pairs` として作る。
  - **理由**: **これが m=12 を保つ唯一の方法**。スロット全体をやり直すと**既回答分のペアが二重に判定され、該当 item の比較数が 12 を超えて露出 gap≠0 になる**（＝事前生成の前提そのものを壊す）。
  - 未回答ペアは `judgments`（token, pair_id）との差分で導出できる。
  - **評価者効果の分析時に「当該スロットは 2 名」と注記**（BR-U6-15）。
- **B**: スロット全体をやり直す。→ **露出均衡が壊れる**（上記）。不採用。
- **C**: 脱落分は欠損のまま（補充しない）。→ 該当 item の m が 12→11 等に低下。BT は成立するが均衡が崩れる。**補充できるならすべき**。

[Answer]: **A ＋ 練習ペアの例外を明記（BR-U6-15 に追記済み）**。未消化のみ引き継ぎが m=12 を保つ唯一の方法、という論証はそのとおり。**ただし補充トークンは別人**なので、**元評価者が練習を消化済みでも練習ペアは必ず全量再提示する**（**読み返しテストの習得なしに本番を判定させない**）。練習判定は出力段除外ゆえ**二重カウントの害はゼロ**。→ 「**引き継ぎ＝本番の未回答ペアのみ / 練習ペアは常に全量**」を BR-U6-15 に明記。

### Q5【activate ガードの実装形態 + 適用性評価】
- **★A（推奨）**:
  - **`activate` 切替の拒否は「アプリ層の事前検証」**で実装する（U6-NFR-20）。**判定条件**: **有効セットに紐づく judgment が 1 件でも存在したら拒否**。
    - **DB 制約では表現できない**（`judgments` と `assignment_plan_meta` をまたぐ条件）。
    - **拒否は 4xx で明示**し `admin_log` に記録（何を試みて拒否されたかが証跡に残る）。
  - **`is_active` の一意性**は **`assignment_plan_meta` の部分ユニークインデックス** or アプリ検証（NFR Design Part 2 で実装形態を確定）。
  - **適用性**: キャッシュ / キュー / CB / 分散ロック / スケール = **N/A**（プラン引き当ては PK 参照・運用者の逐次操作）。**インデックスは `assignment_plan` の PK のみ**（プール 38・プラン 228 行×2 セット）。
- **B**: activate 拒否を実装しない（運用注意のみ）。→ **実験の同一性に直結する操作**を無防備にする。不採用。

[Answer]: **A ＋ トークンへの `plan_set` 束縛で競合窓を消す**。judgment 存在で拒否・アプリ層事前検証・4xx + `admin_log`、いずれも同意。
  **述語を堅くする追加**: トークンが `plan_index` のみ持ち `plan_set` は「**その時点の有効セット**」を参照する形だと、**発行 → セッション開始の間に activate が切り替わるとトークンの意味が変わる競合窓**がある。→ **発行時に `(plan_set, plan_index)` の組をトークンへ束縛**し、**セッション開始はトークン自身の `plan_set` を読む**（有効セット参照をしない）形にすれば窓が消え、**activate ガードの述語も「当該セットを参照するトークン / judgment の存在」として一意に定まる**。成立版/フォールバック版の切替も「**どちらのセットで発行したか**」がトークン単位で証跡に残る利点付き。→ **Part 2 で実装形態を確定**。

---

**回答後の流れ**: 曖昧点を点検（あれば追加質問）→ Part 2 で `nfr-design-patterns.md`（DP-U6-NN）/ `logical-components.md`（LC-U6-NN + 依存方向）を生成 → 標準 2 択（Request Changes / Continue → **Infrastructure Design〈U6〉**）。回答は本 plan の各 `[Answer]:` 欄へ書き戻す。


---

## Part 2 生成時の記録（2026-07-20）

**全 5 問 A + 追加 5 点**を反映。DP-U6-01〜08 / LC-U6-01〜14 を生成。

**★レビュー指摘への対応（BR 追補 3 件を Part 2 生成前に実施）**:
- **BR-U6-21（内容制約の入力チャネル）**: **①〜⑥は形式制約だけで閉じており、タスク5 §3 の内容制約が生成器仕様から欠落していた**。4 種別（禁止/忌避/濃縮/提示順）を**巡回グラフの幾何に写す**方針で BR 化。**制約ファイルを CLI 入力**とし「中身は研究側・器は U6」の切り分けを保つ。**実測で 3 目的の両立を確認**（禁止辺 8 本違反 0 / 層間 0.706 / 濃縮 9-9 本）。
- **BR-U6-22（期待組成の入力検証）**: `anchor` を `REQUIRED_LAYERS` から外した**裏返し**として、投入忘れをランタイムが拾わなくなる → **`plan_generate` CLI で期待組成を検証**。
- **BR-U6-15 に練習ペアの例外を追記**: 引き継ぎ＝本番未回答分のみ / **練習は常に全量再提示**（補充者は別人ゆえ読み返しテストの習得が必要・出力段除外で二重カウントの害ゼロ）。

**設計への構造的影響**: 内容制約の受け皿として **LC-U6-02 `placement`（制約付き探索）** と **LC-U6-05 `sequencing`（順序付け）** が独立コンポーネントとして必要になった（当初の「構成→検証→リトライ」だけでは表現できない）。

**Part 2 で確定した追加事項**: 監査再現用に **初期 seed + 成功試行番号 + 内容ハッシュ**を記録（DP-U6-07）。**トークンへの `(plan_set, plan_index)` 束縛**で activate 競合窓を構造的に除去（DP-U6-06）。

**次**: 標準 2 択（Request Changes / Continue → **Infrastructure Design〈U6〉**）。
