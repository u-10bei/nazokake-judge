# U6 Business Rules — 層拡張（下帯アンカー）+ 事前生成割当

**背景**: 実データ確定（**n=38 / E=8 / J=228 / m=12**）。U1〜U5 を前提に U6 固有の規則を **BR-U6-NN** で番号付けする。FD 全 7 問（Q1=B / Q2=D / Q3=A+2 / Q4=A′ / Q5=A′ / Q6=A / Q7=A、2026-07-20）。

**確定した設計定数**

| 項目 | 値 |
|---|---:|
| プール規模 n | **38** |
| 評価者数 E | **8** |
| 総判定数 J | **228**（29×4 + 28×4） |
| 1 作品あたり比較数 m | **12** |

---

## 層の拡張

| ID | ルール | 根拠 |
|---|---|---|
| **BR-U6-01（第5層値 `anchor`・役割ベース命名）** | `items.layer` に **`anchor`（表示名「下帯アンカー」）** を追加する。**役割ベースの命名を採る**。<br>**出自ベース（`classic`）を採らない理由**: **S04・S10 も鈴木1963 の人手古典でありながら `pro` 層に居る**ため、出自では S03・S13 を S04・S10 から**区別できない**。区別している実体は**役割**（尺度の床を定義する指名アンカー vs バー本体）。さらにフォールバック発動時は S02・S19・S11・S12（全て古典）が `pro` に入るため、**`classic` 層の横で古典が `pro` に居る**混乱が生じる。<br>**正本文書（structure_labels・タスク5メモ層表）が既に「下帯アンカー」を層名として使用**しており、enum を合わせることで研究⇔実装の対応表が不要になる。 | Q1=B |
| **BR-U6-02（`anchor` を層順序予測に含めない）** | 層順序予測（**機械組立 < 自由生成 < 編集 ≦ プロ**）の検証対象は `rule`/`ai`/`edit`/`pro` の 4 層。**`anchor` は役割層ゆえ順序予測に含めない**。 | Q1 |
| **BR-U6-03（バーの定義＝指名アンカーの β 位置）** | **「プロ水準」の定義は層平均ではなく、指名アンカー項目（S04・S10・N 群）の β 位置**とする。**層平均は層順序予測（BR-U6-02）の検証にのみ用いる**。→ 万一の層割当ミスの被害を限定する規律。 | 分析原則 |
| **BR-U6-04（練習専用層 `practice`）** | 練習素材（開示セット: S01 / S06 / S07 / S20 / 床側こうしょう）は**本番プールから分離**するため **`layer='practice'`** を与える。練習は出力段で除外済み（`is_practice=1`）ゆえ**分析的に不活性**。 | Q5=A′ |
| **BR-U6-05（★充足ゲートは `POOL_LAYERS` の明示定数で検査）** | プール充足判定（BR-U4a-05 ②「4 層すべて非空」）は **`for layer in Layer` の enum 走査をやめ、本番層の明示定数 `POOL_LAYERS = (pro, ai, edit, rule, anchor)` に対して検査**する。<br>**理由**: enum 走査のままだと**層値を足すたびに「非空」要求が自動で増える**。`practice` を足すと「**practice 非空**」まで要求する誤動作になる（練習素材の有無が本番プールの充足を左右してしまう）。**充足の母数からも `practice` を除外**する。 | Q5=A′ / 事実 #3 の着地 |

## Likert 較正アンカー

| ID | ルール | 根拠 |
|---|---|---|
| **BR-U6-06（Likert は固定リスト全固定運用・コード変更なし）** | **Likert 対象はプラン付属の固定リスト**（`AssignmentParams.likert_fixed_targets` に 10 件すべてを指名）で運用する。<br>**既存実装のまま解決する**: `select_likert_targets` は固定アンカーを**最優先で採用**し、**want 件に達した時点で即 return** する（`likert.py` L32-37）。層ラウンドロビンは**不足分の補充専用**ゆえ、10 件全指名なら**一度も走らない**。<br>`models.py` の「方針（全固定/混合/全ランダム）はプール凍結時に確定（Negotiable）」は **本 BR で「全固定」に確定**。 | Q2=D |
| **BR-U6-07（下帯アンカーを Likert から除外しない）** | **`anchor` 層を Likert 対象から機械的に除外しない**。設計メモの**全帯被覆（低〜高）方針**に従い、低帯の較正点を確保する（**欠くと回帰が下端で外挿になる**）。<br>「下帯 2 件が較正の 20% を占める」歪みは**ラウンドロビンの機械配分が原因**であって下帯の存在が原因ではない。**基準に基づく指名選定**（例: 床1+RB1 で低帯 2 枠）なら**意図した範囲被覆**である。 | Q2=D |
| **BR-U6-08（Likert 10 件の選定はタスク5 論点3へ委譲）** | 具体的な 10 件の選定基準（**全帯被覆 + 権利クリア + 診断標本**）は**研究側の意思決定**であり、システムは**固定リストを受け取るだけ**。U6 は「固定リストで運用する」ことのみを定める。 | Q2=D |

## 事前生成割当（★中核）

| ID | ルール | 根拠 |
|---|---|---|
| **BR-U6-09（完全静的な事前生成設計）** | 参加者フローの割当は**事前生成した固定プラン**を引き当てる。**実行時に割当ロジックを走らせない**。<br>**設計の性質**: J=228 は n=38 の **12-正則グラフ**（辺数 38×12/2 = 228）と完全一致し、**露出の相対 gap = 0**（全 item がちょうど m=12）。8 スロットへの分割 [29,29,29,29,28,28,28,28] が構成可能であることを実測確認済み。 | Q3=A |
| **BR-U6-10（プランの制約・検証必須項目）** | 生成されたプランは次を**すべて**満たすこと（検証は PBT, BR-U6-17）:<br>① **露出均衡**: 全 item の出現回数が厳密に m=12（gap=0）<br>② **連結性（全体）**: プラン全体（全 J 辺）の比較グラフの連結成分が **1**<br>③ **k 制約**: 評価者内の同一 item 出現 ≤ 3<br>④ **同一ペア再提示ゼロ**: 同一評価者に同一ペアを出さない<br>⑤ **層間比率**: 層間ペア ≥ 0.65<br>⑥ **★連結性（ブロック単位）**: **各ブロック（4 スロット）の辺和集合だけで連結成分が 1**（BR-U6-20） | Q3=A / Q6=A |
| **BR-U6-21（★内容制約の入力チャネル・追補 2026-07-20）** | プラン生成器は**形式制約（BR-U6-10 の①〜⑥）だけでなく、研究側が定める内容制約を入力として受け取り遵守する**。**制約の中身は研究側（タスク5 §3）の成果物、器は U6** という切り分けを保つため、**制約ファイルを CLI 入力**とする。<br><br>**制約の 4 種別**:<br>① **辺の禁止（ハード）**: 指定ペアの**直接対戦を禁止**（例: pivot 音衝突ペア・全46音照合）。→ **円周距離 >6 に配置することで構成的に排除**（`C_n(1..6)` の隣接は距離 ≤6 のため）。**PU6-8 で不在を検証**。<br>② **辺の忌避（ソフト）**: 可能なら避ける（例: 準同音）。**排除できなくても失敗させず、レポートに残す**。<br>③ **辺の濃縮**: 指定 item 間の直接対戦を**厚くする**（例: 診断標本を厚めに）。→ **配置探索の目的関数**。<br>④ **提示順の回避**: 同一評価者内で**指定グループを連続提示しない**。→ **辺集合ではなくペア列の順序**の制約ゆえ、**分割後に順序付け（sequencing）ステップ**が必要。<br><br>**設計への含意**: 配置（placement）は**単なるインターリーブではなく制約付き探索**になり、**分割後に順序付けが 1 段増える**。<br>**実測（成立性確認済み）**: 禁止辺 8 本・濃縮対象 9 件の条件で近傍探索（seed 固定）した結果、**禁止辺の違反 0 / 層間比率 0.706（≥0.65 充足）/ 濃縮 9-9 本**を**同時に達成**。3 目的は両立する。<br><br>**★`plan_generate` の入力は「三つ組」であり、セット単位で異なる（2026-07-20 明文化）**: **（プール・期待組成・制約ファイル）**。**制約ファイルも成立版とフォールバック版で別物**であり、成立版のものを流用できない:<br>　(i) **pivot 衝突照合**: フォールバック版は **S02・S11・S12・S19 の音を含む 34 件で独自に再走**が必要（**成立版の 46 音表は流用不可**）<br>　(ii) **N8 濃縮制約は存在しない**（結節が S04・S10・例11・例13〜15・新作①〜④ の 11 件構成に変わり、**診断標本も N8 抜きの 3 件**）<br>　(iii) **N 系の隣接回避制約も消滅**（N6×新作③・N7×C-6）<br>→ **フォールバック版の制約ファイルは n=34 前提で別途作成**する（タスク5 側の TODO）。 | タスク5 §3 / レビュー指摘 |
| **BR-U6-22（★期待組成の入力検証・追補 2026-07-20）** | プラン生成器は**期待組成**（`n` と層別件数、例: 38 = pro10 / anchor2 / edit14 / ai9 / rule3）を**引数で受け取り、実プールとの不一致で明示失敗**する。<br>**理由**: `anchor` を `REQUIRED_LAYERS` から外した（BR-U6-05 / NFR Design Q1）**裏返しとして、`anchor` 投入忘れのまま本番プランが生成される事故をランタイムのゲートが拾わなくなる**。この検査の正しい置き場は**ランタイムではなく `plan_generate` CLI**（研究側入力の一部として BR-U6-21 の制約ファイルと同じ層に載る）。 | NFR Design Q1 の裏返し / レビュー指摘 |
| **BR-U6-20（★ブロック単位の連結性・追補 2026-07-20）** | **各ブロック（4 スロット = 前半/後半）の辺和集合のみで比較グラフが連結**（成分 1）であること。<br>**根拠**: **BR-U6-13 で存続を確定した (b) 推定の逐次更新の前提**。ブロック1 単体で暫定 BT を出すには**ブロック1 の比較グラフが連結**でなければならない（非連結なら最大成分しか推定されず、**全項目の暫定位置が出ない**）。<br>**「自然に満たされるが保証ではない」（実測）**: 貪欲配分 30 試行でブロック1・2 とも成分 1（116 辺 ≫ 連結に必要な 37 辺）。**しかし構成上の保証ではない**ため、E・分割・構成法を変えると静かに壊れうる。→ **生成時の明示的な検証項目**とし、PBT（PU6-7）で反例探索する。<br>**BR-U6-10 ⑥ として収載**。 | BR-U6-13 (b) の前提 / レビュー指摘 |
| **BR-U6-11（決定論・監査再現性）** | プラン生成は**決定論**（seed を記録）。同一 (プール, E, J, seed) → 同一プラン。**投入前にプランを検査できる**ことが事前生成の要件（実行時生成では事前検査が不可能）。 | Q3=A |
| **BR-U6-12（2 プランセット・★改訂 2026-07-20: リポジトリ固定 / D1 は単一セット）** | **成立版**（N1〜N8 許諾あり）と**フォールバック版**（N1〜N8 を置換）の **2 セットを事前生成する**が、**格納場所を分ける**:<br><br>**① 両セットは「リポジトリにコミット」して固定する**（プラン本体 + `seed` + 内容ハッシュ + 制約検証レポート）。**「許諾判明前に両設計が固定されていた」証跡は commit 履歴とハッシュが担う**（事前記録の思想と同型。**git のタイムスタンプと不変性は D1 より強い証跡**）。<br>**② D1 には選択されたセットのみを投入する**。カットオーバー時に**該当プールを ingest → 該当セットを POST → activate**。`admin_log` の `plan_ingest` に記録された内容ハッシュが**コミット済みハッシュと一致**することで**内容の同一性が閉じる**（DP-U6-07）。<br><br>**なぜ「両セット D1 投入」を廃止するか（★改訂理由）**: **D1 内でセットを切り替える時間窓が存在しない**——<br>　(i) **activate ガード**（judgment 1 件で切替拒否, U6-NFR-20）により**切替は収集開始前にしか起こり得ない**<br>　(ii) **カットオーバーは許諾判明後**ゆえ、**pool_ingest の時点でどちらのセットで走るかは確定済み**（**両セットで `items` の組成が異なる**ため、そもそもプールを入れる前に決まっている必要がある）<br>　→ **両セット同時保持は「使われない自由度」にコストだけ払う**。しかも**非活性セットの item を `items` に置く必要が生じ**、期待組成チェック（BR-U6-22）と衝突し、**activate 時のプール入替は凍結ガード（BR-U4a-03）とも衝突**する。<br><br>**`activate` エンドポイントは存続**（廃止しない）: **投入と収集開始の間に置く明示的な「武装」ステップ + ガード**として機能する。<br>**★セット別の確定値（2026-07-20 確定・実測で構成可能性を検証済み）**:

| 項目 | **成立版** | **フォールバック版** |
|---|---|---|
| `n` | **38** | **34** |
| 層別 | pro10 / anchor2 / edit14 / ai9 / rule3 | **pro6**（S04・S10・S02・S11・S12・S19）/ anchor2 / edit14 / ai9 / rule3 |
| `J`（= n·m/2, m=12） | **228** | **204** |
| 分割 | [29×4, 28×4] | **[26×4, 25×4]** |

**フォールバック予約は S02・S11・S12・S19 のちょうど 4 件**（タスク2集計・許諾成立まで凍結）。**38 − 8 + 4 = 34**。
**n=36 へ寄せない**（[27×8] の均一分割は**美観であって測定上の利得ではない**）: (i) 成立版が既に 29/28 混在で走る設計であり [26×4, 25×4] も同質。**m=12・連結性・gap=0 は n=34 で全て成立**（実測: 辺数 204・次数 gap=0・連結成分 1・充足ゲート通過・層間 0.716）(ii) **自由在庫（S08・S14・S15・S17）は「初回 BT で中間帯の推定が不安定だった場合の追補口」として温存が確定済み**——**劣化条件下（フォールバック＝バーが薄い）でさらに予備を減らすのは悪手** (iii) 中間帯古典は `anchor`（床の役割）でも `pro` でもなく、**層割当の議論をやり直すことになる**（フォールバック発動時にやる作業ではない）。<br>**収集開始後の許諾 NG** は **U5 の `pool_retire`** で将来の出題のみ止める（過去判定は有効・エクスポートに本文は含まれない）。**プランセットの切替では対応しない**。 | Q3=A 追加要件1 / タスク5 §4 / Infra Q1=A′ |
| **BR-U6-13（★適応的再生成の不採用＝完全静的化）** | **「ブロック 1 の結果でブロック 2 を再生成」する適応的サンプリングは採用しない**。<br>**根拠**: **FR-03 が既に決着済み**——「適応的サンプリングは v1 では採用せず、将来の反復判定フェーズで再検討する」（要件 L52・非目標表 L162）。検証質問の選択肢 C（「これまでの結果をもとに情報量の多いペアを動的に選ぶ」）が不採用とされ Q2=X（計画的制約付きランダム）が確定している。**完全静的化は新たな設計変更ではなく既決要件への復帰**。<br>**廃止対象の切り分け（★2026-07-20 確定）**: 廃止するのは **(a) 設計の再生成**のみ。**逐次更新は (b) 推定の逐次更新として存続**（ブロック1で暫定 BT → ブロック2 込みで再推定＝**分析側のみ**）＝**静的設計と完全に両立**。設計メモ §4 の改訂は「再生成」の文言のみを対象とし、**逐次更新の構想自体は (b) として保持**する。<br>**(b) の前提 = BR-U6-20（ブロック単位の連結性）**: ブロック1 単体で暫定 BT を出すには**ブロック1 の比較グラフが連結**である必要がある。プラン全体の連結性（BR-U6-10 ②）では不十分ゆえ**独立の検証項目**とする。<br>**トークン発行の 4+4 運用（段階的募集）は維持**（設計に影響しない運用上のペース配分）。 | Q3=A 追加要件2 / FR-03 |

## スロット運用（ドロップアウト）

| ID | ルール | 根拠 |
|---|---|---|
| **BR-U6-14（8 スロット固定・予備プランを作らない）** | スロットは **8 で固定**する。**「E=10 分を生成して先着 8 名分を使う」方式は採らない**——J=228 は 8 スロットへの**完全分割**であり、10 分割（≈23 ペア/枠）で 8 枠のみ消化すると **44 辺が未消化**となり **12-正則が壊れる**（均衡の前提そのものを破壊する）。 | Q4=A′ |
| **BR-U6-15（脱落時は同一スロットの補充トークン）** | 脱落時は**同一 `plan_index` に紐付く補充トークン**を発行し、**未消化ペアを引き継ぐ**。<br>judgments は **token 単位**ゆえ 1 スロットに 2 評価者が入っても **BT 集計は不変**（item 単位の比較数 m=12 が保たれる）。**評価者効果の分析時に「当該スロットは 2 名」と注記**する。<br>**引き継ぎの粒度（★2026-07-20 確定）**: **引き継ぎ対象は「本番の未回答ペア」のみ**（既回答分を再提示すると**該当 item の比較数が 12 を超え露出 gap≠0 になり事前生成の前提が壊れる**）。<br>**ただし練習ペアは常に全量を再提示する**——**補充トークンは別人**であり、**読み返しテストの習得なしに本番を判定させてはならない**。練習は出力段で除外されるため**二重カウントの害はゼロ**（`is_practice=1`）。 | Q4=A′ / NFR Design Q4 |

## 練習・初見性

| ID | ルール | 根拠 |
|---|---|---|
| **BR-U6-16（練習素材は本番プールから完全分離）** | 練習ペアは**開示セット**（S01 / S06 / S07 / S20 / 床側こうしょう・**2026-07-18 承認・自作消費ゼロ**）を使い、**本番 38 件を一切使わない**。<br>**理由**: **埋蔵性は初見でしか測れない**（pivot は一度見たら掘り出し済みで、2 回目以降は「機知の発見」でなく「記憶との照合」になる）。練習で本番項目を消費すると**その初見性は回復しない**。<br>**練習ペアはプランに固定記載**（**全評価者共通**・`is_practice=1`）。→ **本番 38 件すべての初見性が保たれる**。 | Q5=A′ |

## 検証・分析仕様

| ID | ルール | 根拠 |
|---|---|---|
| **BR-U6-17（既存 PBT をプラン検証へ流用）** | `generate_pairs` の既存 PBT（P-1 露出均衡・層間比率・k 制約）を**事前生成プランの検証にそのまま流用**する。**`generate_pairs` は削除しない**（参加者フローからは呼ばれなくなるが、資産・検証・将来の選択肢を残す）。<br>**α/S 較正（p=3・α=0.7・S=30）は「参加者フローでは不使用」と明記**し、`generate_pairs` に紐づく記録として保存する（n=95 での較正値ゆえ n=38 では保証されないが、**事前生成では較正自体が不要**）。 | Q6=A |
| **BR-U6-18（同一ペア再提示ゼロはハード制約・明文化）** | **同一評価者への同一ペア再提示はゼロ**。既存実装は `used: set[frozenset]` で**構造的に保証済み**（`assignment.py`）であり、事前生成でも**設計段階で保証**される（全 228 辺が相異なる）。**仕様として明文化**する（実装追加は不要）。 | Q7=A / 事実 #6 |
| **BR-U6-19（出現回数は導出・スキーマ追加なし）** | 「その評価者にとって当該 item が**何回目の出現か**」は **`token` + `pair_index` から分析時に導出可能**（token 内で `pair_index` 順に累積カウント）。**`judgments` へのカラム追加はしない**（導出可能な値を冗長に持つと整合性リスクが増える）。<br>**分析計画の必須確認項目**: 「**出現回数の主効果が β 推定に乗るか**」を検証する（初見判定と再見判定の系統差は交絡であると同時に、**読み返し基準側に寄る**という codebook との整合もあるため、共変量として明示的に扱う）。 | Q7=A / 事実 #8 |

---

## 検証・エラー処理サマリ

| 状況 | 挙動 |
|---|---|
| `layer='anchor'` の投入 | **許可**（migration 0005 の CHECK に追加） |
| `layer='practice'` の投入 | **許可**。ただし**充足の母数には入らない**（`POOL_LAYERS` 外, BR-U6-05） |
| `practice` 層が空 | **充足ゲートに影響しない**（enum 走査をやめたため, BR-U6-05） |
| `anchor` 層が空 | `POOL_LAYERS` に含むため**非空を要求**（→ 要否は NFR Design の決定点） |
| Likert 固定リストが 10 件未満 | 不足分はラウンドロビン補充（既存挙動）。**全固定運用では起こさない**（BR-U6-06） |
| プランが BR-U6-10 の①〜⑤を満たさない | **投入前に検出**（生成 CLI が検証・PBT が反例探索, BR-U6-11/17） |
| 評価者の脱落 | 同一 `plan_index` の補充トークンで未消化分を引き継ぐ（BR-U6-15） |
| 収集開始後の許諾 NG | **プランセットは切り替えない**。U5 `pool_retire` で将来の出題のみ停止（BR-U6-12） |
| 練習で本番項目が出る | **起こらない**（練習素材は本番プールと完全分離, BR-U6-16） |
